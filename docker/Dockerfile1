# 使用 Python 3.8 的 slim 版本作为基础镜像
FROM python:3.8-slim

# 设置工作目录
WORKDIR /app

# 将 paddle 文件夹的内容复制到容器的 /app 目录中
COPY ./paddle /app

# 安装项目所需的依赖
RUN pip install --no-cache-dir -r requirements.txt

# 设置 Flask 环境变量
ENV FLASK_APP=back-end-api.py
ENV FLASK_RUN_HOST=0.0.0.0

# 暴露端口 8888
EXPOSE 888

# 运行 Flask 应用
CMD ["flask", "run", "--port=888"]
